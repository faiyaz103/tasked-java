server:
  port: 8080

spring:
  application:
    name: modular
  datasource:
    url: jdbc:postgresql://localhost:5432/tasked_java
    username: postgres
    password: 1234
    driver-class-name: org.postgresql.Driver
  
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update